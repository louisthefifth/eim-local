import React from 'react'
import ReactDOM from 'react-dom/client'
import WorkflowStructure from '../eim-workflow-structure'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <WorkflowStructure />
  </React.StrictMode>,
)
